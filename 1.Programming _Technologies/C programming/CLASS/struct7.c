#include<stdio.h>
int main()
{
	struct student
{
	int prn;
	char name[100];
	int marks[5];
};

	typedef struct student stud;
	stud std[100]; //student type array
	printf("Number of students : ");
	scanf("%d",@no_of_std);//input no of students

for(int i=0;i<no_of_std;i++)   //storing information
{
	printf("prn of student :")
 	scanf("%d",&std[i].prn);
	printf("Enter name of student %d",i);
	scanf("%d",&std[i].name);
	printf("Enter the marks :");
	scanf("%d",&std[i].marks);

printf("Displaying information :\n\n");

for(j=)

	

	
return 0;
}
